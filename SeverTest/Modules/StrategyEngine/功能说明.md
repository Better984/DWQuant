# StrategyEngine 模块功能说明

## 功能概述
- 策略核心执行引擎与指标计算能力。
- 条件评估、动作队列与运行日志写入。
- 条件优先级剪枝：Cross/Required/Optional 顺序评估，达到 MinPassConditions 可提前短路（不改变 Required/Optional 语义）。
- 启动前运行检查（API Key、仓位模式、余额等）。
- 条件检测取值日志：记录指标值、K 线时间与开高低收、固定值等（当前已在代码中统一注释，默认不输出，仅在排查条件取值问题时可临时打开对应 LogInformation 调用）。
- 策略运行时间门禁：支持模板/自定义时间段，控制策略在实盘中的执行窗口。
- 开仓筛选器：仅在开多/开空检测前执行，用于快速过滤不满足条件的策略。
- 运行时间模板由数据库存储，服务端提供模板接口与缓存；策略仅保存模板 ID；模板可挂载交易日历，用于节假日/异常交易时段。
- 运行时执行用户统一取 `user_strategy.uid`，避免分享/导入场景误用 `strategy_def.creator_uid` 导致实盘执行账号错误。
- 动作队列支持按 `usId` 封禁，配合删除/下线流程阻断残留动作继续执行。

## 条件检测支持
- 比较方法：`GreaterThan`、`GreaterThanOrEqual`、`LessThan`、`LessThanOrEqual`、`Equal`、`NotEqual`。
- 交叉方法：`CrossUp`、`CrossDown`、`CrossAny`（兼容 `CrossOver` / `CrossUnder` 别名）。
- 区间方法：`Between`、`Outside`。
- 持续方法：`Rising`、`Falling`、`AboveFor`、`BelowFor`。
- 变化方法：`ROC`、`Slope`。
- 通道边界：`TouchUpper`、`TouchLower`、`BreakoutUp`、`BreakoutDown`。
- 统计方法：`ZScore`、`StdDevGreater`、`StdDevLess`。
- 带宽趋势：`BandwidthExpand`、`BandwidthContract`。
- 参数来源：
  - `args`：值引用（Field / Indicator / Const），用于左右值或多参数取值。
  - 新增 `args.RefType=PublicIndicator`：引用“公共指标”字段，不依赖交易所/交易对/周期，可被所有策略复用。
    - `Indicator`：公共指标 code（示例：`coinglass.fear_greed`、`coinglass.etf_flow`）。
    - `Input`：scope（可填 `global` 或 `k=v&k2=v2`）。
    - `Output`：字段路径（示例：`signals.below10Consecutive3d`、`signals.netInflow3dAllPositive`、`signals.netFlow7dSumUsd`）。
  - `param`：辅助参数（如窗口长度 N、阈值），与 `args` 可同时存在。
  - 兼容：当 `args` 不足时，仍可使用 `param` 作为常量补足。

## 主要入口
- 引擎与执行：`Application/RealTimeStrategyEngine.cs`、`Application/QueuedStrategyActionExecutor.cs`
- 指标/条件：`Application/IndicatorEngine.cs`、`Application/ConditionEvaluator.cs`
- 条件缓存清理：`Application/ConditionCacheCleanupHostedService.cs`
- 运行检查与日志：`Application/RunChecks/*`、`Application/RunChecks/Checks/ExchangeReadyRunCheck.cs`、`Application/StrategyEngineRunLogWriter.cs`
- 运行时间配置与判定：`Domain/StrategyRuntimeSchedule.cs`、`Application/StrategyRuntimeConfigValidator.cs`
- 运行时间模板与日历：`Infrastructure/StrategyRuntimeTemplateRepository.cs`、`Infrastructure/StrategyRuntimeTemplateStore.cs`

## 配置选项
- `ConditionCacheOptions`：条件缓存清理间隔配置（`ConditionCache:CleanupIntervalSeconds`）

## 联动关系
- `ExchangeApiKeys`：运行检查读取 API Key 状态。
- `MarketStreaming`：获取行情/指标输入数据。
- `Indicators`：通过 `PublicIndicatorValueProvider` 读取公共指标 latest/history，用于条件判断。
- `TradingExecution`：将策略动作下发到交易执行层。

## 变更记录
- 2026-03-01：新增统一 warmup 规则 `TalibCalcRules.ResolveRequiredBars`，`IndicatorHandle` 与回测 `BacktestRunner` 共用同一口径；EMA 族指标（`EMA/DEMA/TEMA/TRIX/T3/KAMA/MAMA`）最小预热 200 根，降低短历史初始化导致的交叉时点漂移。
- 2026-02-27：修复“指标预计算与执行前同步刷新并发导致同任务重复重算”回归问题：`IndicatorHandle.Update` 在收线同时间戳下直接复用已算结果；`RealTimeStrategyEngine` 同步刷新全部成功后回写预计算水位；`IndicatorEngine.PrecomputeHandlesForTask` 遇到已完成水位直接跳过整任务，降低重复计算与额外 CPU 开销。
- 2026-02-27：`admin.strategy.task.trace.market-summary` 数据源切换为 `strategy_engine_run_log` 主记录；`strategy_engine_run_log` 增加 `trace_id/run_status/lookup_ms/indicator_ms/execute_ms/open_task_trace_ids/open_order_ids` 等字段，交易开仓成功后按 `trace_id` 反写订单ID，支持“最近5次任务 + 任务详情”直接走主表查询。
- 2026-02-27：`StrategyRunMetrics` 计数与策略ID采样并入 `StrategyRunTrace`，统一单次任务画像结构；新增按策略查询最近运行画像仓储查询，供管理端策略详情展示最近 5 条运行明细（成功率、开仓次数、阶段耗时等）。
- 2026-02-26：Phase 4 TALIB WASM 多进程池——新增 `TalibWasmNodePool`，round-robin 分发指标计算到 N 个独立 Node.js 子进程，解除 `TalibWasmNodeInvoker` 单进程全局锁瓶颈；`TalibIndicatorCalculator`/`IndicatorEngine`/`BacktestMainLoop`/`MarketDataTalibCompareController` 统一改用池化调用；`TalibCoreOptions` 新增 `PoolSize` 配置项。
- 2026-02-26：Phase 3 键分片支持——RealTimeStrategyEngine 新增 `ProcessTask`/`AdjustInnerParallelism` internal 接口，配合 `KeyShardedStrategyConsumer` 实现异键并行消费。
- 2026-02-26：Phase 2 指标异步预计算——IndicatorEngine 新增 `SubscribeAndAutoUpdateAsync`，订阅行情引擎自动预计算指标，HandleTask 的同步指标刷新变为缓存命中 fallback。
- 2026-02-26：Phase 1 续——HandleTask 5 个 Stopwatch → `Stopwatch.GetTimestamp()` 时间戳链；StrategyRunMetrics 的 ConcurrentDictionary → ConcurrentBag；UpdateIndicatorsBeforeExecute 的 LogDebug 加诊断守卫。
- 2026-02-26：Phase 1 首批——新增 `LiveTradingObjectPoolManager` 池化 Context/List；RealTimeStrategyEngine 热路径零分配改造 + LogDebug 守卫。
- 2026-02-25：新增 `strategy_task_trace_log` 全链路追踪（任务处理方、阶段状态、耗时与明细），并打通行情分发/策略执行/动作入队/交易消费四段 trace。
- 2026-02-25：移除实盘主链路中的 testing 条件逐条写库逻辑（`test_strategy_check_log` 写入点），减少执行路径冗余开销。
- 2026-02-24：新增策略运行画像日志与 `extra_json` 分段明细，支持按阈值输出/落库（查找、指标刷新、执行分段 + 单策略执行统计）。
- 2026-02-23：指标队列新增收线任务关键补写（有界补写队列 + 固定后台 worker），并在配置为 `DropOldest/DropNewest` 时自动校正为 `DropWrite`，降低高压下关键指标刷新任务丢失概率。
- 2026-02-23：条件检查测试日志改为仅 `testing` 状态写入，并移除按条件 `Task.Run` 调度，降低实盘高压场景线程池抖动。
- 2026-02-23：修复策略动作队列高压丢单：`StrategyActionTaskQueue` 新增 `EnqueueAsync` 使用 `WriteAsync`，队列满时按 `FullMode=Wait` 等待，`QueuedStrategyActionExecutor` 改用异步入队，避免 TryWrite 失败直接丢单。
- 2026-02-23：策略执行改为“小批量串行 + 大批量并行”的混合模型，减少低匹配场景并行调度开销，提升高并发下延迟稳定性。
- 2026-02-02：修复条件日志中指标 Timeframe 的取值来源，统一从指标请求键读取。
- 2026-02-02：条件检测 K 线日志补充 open/high/low/close，便于排查取值。
- 2026-02-05：运行时间模板改为模板 ID + 服务端日历解析，支持节假日/异常交易时段。
- 2026-02-05：新增策略运行时间门禁，支持模板/自定义时间段控制实盘执行。
- 2026-02-05：运行时间模板改为数据库存储，新增后台管理接口与缓存刷新。
- 2026-02-05：新增开仓筛选器配置与优先判断流程。
- 2026-02-06：条件评估引入优先级剪枝（Cross/Required/Optional 顺序，保持语义不变）。
- 2026-02-23：修复运行时执行用户映射，动作入队用户统一使用策略实例所属用户（`user_strategy.uid`）。
- 2026-02-23：动作队列新增策略封禁能力，为“删除后立即停机”提供残留任务阻断。
