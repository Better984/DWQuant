# Shared 模块功能说明

## 功能概述
- 提供通用基础设施：数据库、缓存、对象存储、协议封装、公共模型、中间件与限流。
- 提供服务器配置中心（数据库存储 + 启动初始化 + 运行时读取）。
- 提供服务器角色启动引导（核心节点 / 回测算力节点 / 全功能节点 / 实盘服务器）与本地默认角色持久化。
- 角色引导支持三步交互：选择模式、选择是否更新分布式核心节点地址（`|` 分隔）、确认启动。
- HTTP 限流支持“登录态按 uid、匿名按 IP、登录/注册按独立 auth 路径+IP”三类键，降低匿名暴力请求风险。
- 匿名限流在 `RemoteIp` 缺失时增加兜底地址解析（`X-Forwarded-For`/`X-Real-IP`/`ConnectionId`），避免匿名请求旁路限流。
- CORS 默认策略改为白名单来源（`Cors:AllowedOrigins`），未配置时仅回退本地开发地址。
- 新增进程实例标识提供器与链路追踪基础设施（`StrategyTaskTraceLogQueue/Writer/Repository`），支持跨模块统一记录任务处理路径与耗时。
- 作为各业务模块的基础依赖层。

## 主要入口
- 服务：`Application/Services/DatabaseService.cs`、`Application/Services/RedisCacheService.cs`、`Application/Services/OSSService.cs`、`Application/Services/ServerConfigService.cs`
- 链路追踪：`Application/Diagnostics/StrategyTaskTraceLogQueue.cs`、`Application/Diagnostics/StrategyTaskTraceLogWriter.cs`、`Infrastructure/Diagnostics/StrategyTaskTraceLogRepository.cs`
- 角色与启动：`Application/Services/ServerRoleBootstrap.cs`、`Application/Services/ServerRoleRuntime.cs`、`Application/Services/SystemStartupManager.cs`
- 配置中心：`Infrastructure/Config/ServerConfigRepository.cs`、`Infrastructure/Config/ServerConfigStore.cs`
- 协议与过滤器：`Protocol/*`、`ProtocolRequestFilter.cs`、`ProtocolResponseFilter.cs`
- 中间件：`Middleware/ExceptionHandlingMiddleware.cs`、`Middleware/SystemReadinessMiddleware.cs`、`Middleware/HttpTokenValidationMiddleware.cs`
- 选项与校验：`Options/*`、`Options/Validation/*`

## 配置选项
- `DbOptions`：数据库连接与慢查询阈值
- `HistoricalMarketDataOptions`：历史行情同步与预加载
- `RuntimeQueueOptions`：运行时队列容量与背压
- `ConditionCacheOptions`：条件缓存清理间隔
- `StrategyDiagnosticsOptions`：策略链路分段耗时画像与慢日志阈值
- `RuntimeQueueOptions.StrategyTaskTraceLog`：链路追踪日志异步落库队列
- `StrategyOwnershipOptions`：策略租约与协调
- `WebSocketOptions`：WebSocket 路径、消息大小、连接数与 Redis Key TTL
- `RateLimitOptions`：HTTP/WS 限流
- `StartupOptions`：启动流程超时与预热
- `TradingOptions`：交易模式
- `NotificationOptions`：通知投递与重试
- `BusinessRulesOptions`：业务规则常量（角色、分享码、API Key 上限）
- `MarketDataQueryOptions`：行情查询/拉取相关限制
- `RedisKeyOptions`：Redis Key 命名规范
- `MonitoringOptions`：监控日志条目上限与桌面宿主开关（`EnableDesktopHost`）
- `RequestLimitsOptions`：请求体/协议相关限制
- `BacktestWorkerOptions`：分布式回测算力节点连接、心跳与分发间隔（含多核心地址列表 `CoreWsUrls`）
- `LiveTradingWorkerOptions`：分布式实盘节点连接与心跳配置（含多核心地址列表 `CoreWsUrls`）

## 联动关系
- 全部业务模块：共享数据模型、协议封装、基础设施与中间件能力。
- 全部需要登录的 HTTP 接口：统一经过 `HttpTokenValidationMiddleware` 做 token 有效性兜底校验，避免旧会话 token 在被挤下线后继续访问接口。
- `Backtest`：通过 `ServerRoleRuntime` 控制核心节点/算力节点功能开关，并通过 `BacktestWorkerOptions` 约束分发与心跳行为。
- `Backtest`：算力节点连接目标可通过 `ServerRoleBootstrap` 交互式录入并持久化，在启动时覆盖 `BacktestWorker:CoreWsUrls/CoreWsUrl`。
- `StrategyRuntime`：核心节点通过 `LiveTradingWorkerGateway` 接收实盘节点接入，`LiveTradingWorkerDispatchService` 分发策略增删指令，实盘节点通过 `LiveTradingWorkerClientHostedService` 执行并落地到运行时引擎。

## 变更记录
- 2026-02-27：修复 `QueuePressureMonitor` 在 `DropWrite/DropOldest/DropNewest` 模式下的计数虚高问题，改为容量上限饱和计数，避免告警日志出现“当前值显著大于容量”的误导。
- 2026-02-25：新增 `live-trading-worker` 角色与实盘分布式通信链路（`/ws/live-worker` + 指令分发/回执）。
- 2026-02-25：新增统一任务链路追踪基础设施（`strategy_task_trace_log` + 异步写入队列），支持分布式处理方与阶段耗时定位。
- 2026-02-24：新增 `StrategyDiagnosticsOptions` 及校验器，统一控制策略运行画像、行情分发、动作入队与交易消费耗时日志。
- 2026-02-23：HTTP 限流新增匿名 IP 维度与登录/注册独立限流键；CORS 改为读取白名单来源并禁用全开放默认策略。
- 2026-02-23：修复匿名限流旁路边界，`RemoteIp` 为空时改为兜底地址键而非直接跳过限流。
